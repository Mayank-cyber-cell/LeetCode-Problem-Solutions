class Solution {
public:
    int smallestRepunitDivByK(int k) {
        int rem = 1 % k;
        for (int len = 1; len <= k; len++) {
            if (rem == 0) return len;
            rem = (rem * 10 + 1) % k;
        }
        return -1;
    }
};
